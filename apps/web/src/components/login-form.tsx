'use client';import { useState } from 'react';import { t, type Lang } from '../lib/i18n';
export function LoginForm({lang}:{lang:Lang}){const[error,setError]=useState('');return <form className="login panel" onSubmit={async e=>{e.preventDefault();const form=new FormData(e.currentTarget);const response=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:form.get('password')})});if(response.ok)location.href='/';else setError(t(lang,'login.error'));}}><p className="eyebrow">{t(lang,'login.subtitle')}</p><h1>{t(lang,'login.title')}</h1><label>{t(lang,'login.password')}<input name="password" type="password" autoFocus required/></label>{error&&<p className="error">{error}</p>}<button>{t(lang,'login.submit')}</button></form>}
