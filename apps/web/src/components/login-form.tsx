'use client';import { useState } from 'react';
export function LoginForm(){const[error,setError]=useState('');return <form className="login panel" onSubmit={async e=>{e.preventDefault();const form=new FormData(e.currentTarget);const response=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:form.get('password')})});if(response.ok)location.href='/';else setError('Неверный пароль или не настроен env');}}><p className="eyebrow">LOCAL ACCESS</p><h1>Realtor Collector</h1><label>Пароль<input name="password" type="password" autoFocus required/></label>{error&&<p className="error">{error}</p>}<button>Войти</button></form>}
