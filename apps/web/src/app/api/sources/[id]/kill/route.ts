import { NextResponse } from 'next/server';import { getRepositories } from '../../../../../lib/db.js';import { requireApi,apiError } from '../../../../../lib/http.js';
export async function POST(_request:Request,{params}:{params:Promise<{id:string}>}){try{await requireApi(true);const{id}=await params;const repos=getRepositories();const source=repos.sources.get(Number(id));if(!source)throw new Error('source not found');const updated=repos.sources.update(source.id,{killSwitch:!source.killSwitch});repos.audit.record('source.kill_switch','source',source.id,{enabled:updated.killSwitch});return NextResponse.json(updated);}catch(e){return apiError(e);}}
