import { getRepositories } from '../../lib/db';import { getLang } from '../../lib/lang';import { t } from '../../lib/i18n';import { KeywordForm } from '../../components/keyword-form';
export const dynamic='force-dynamic';export default async function Keywords(){const lang=await getLang();const rows=getRepositories().keywords.list();return <><p className="eyebrow">AZ · RU · EN · mixed</p><h1>{t(lang,'keywords.title')}</h1><KeywordForm lang={lang}/><div className="cards">{rows.map(k=><article className="card" key={k.id}><span className="badge">{k.language}</span><strong style={{fontSize:18}}>{k.value}</strong></article>)}</div></>}
